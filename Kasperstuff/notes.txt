Might be an issue that we flipped rows and columns in HiSeq
e.g. -> 
feature_cols = [df.columns.values[x] for x in range(1,len(df)-1)]
X = df[feature_cols] # Features
y = df[df.columns.values[-1]] # Target variable

len(df) is 1144 (# of samples) but the columns are the genes (so 20000) ... not sure